# ── LangChain / LangGraph (v1 LTS) ────────────────────────────────────────────
langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langgraph>=1.0,<2.0
langchain-anthropic>=1.0,<2.0
langchain-openai>=1.0,<2.0

# Adapts the bundled Bullseye MCP server (spawned over stdio) into LangChain tools.
langchain-mcp-adapters>=0.1.0

# Tracing/observability. The default .env enables LangSmith (LANGSMITH_TRACING=true),
# so pin it explicitly rather than relying on it as a transitive dep.
langsmith>=0.3.0

# ── Web server ────────────────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.7

# ── Optional ──────────────────────────────────────────────────────────────────
# Persistent conversation memory (swap MemorySaver in app/main.py):
# langgraph-checkpoint-sqlite>=2.0
# Web search tool (enabled only if TAVILY_API_KEY is set):
# langchain-tavily>=0.1
# Observability:
# langsmith>=0.3.0
