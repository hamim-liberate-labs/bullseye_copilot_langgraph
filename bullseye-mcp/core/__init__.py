"""Core infrastructure for the Bullseye Copilot MCP server: app/config, the
shared HTTP session (transport), and parsing helpers. Feature packages build on
these; nothing here is feature-specific."""
